# Frontend Environment Configuration
# Copy this file to .env.local for local development

# ==================== BACKEND API TARGETS ====================
# Backend API URL (for server-side proxy)
# In dev: http://localhost:9083
# In production: use your backend address (can be same host as frontend via reverse proxy)
API_PROXY_TARGET=http://localhost:9083

# WebSocket URLs (for client-side connections)
# These URLs are used directly by the browser, so must be accessible from client IP
# In dev: ws://localhost:9085 (or ws://your-dev-server-ip:9085)
# In production: typically same domain as frontend via reverse proxy (wss://domain/ws)
NEXT_PUBLIC_MONITORING_WS_URL=ws://localhost:9085
NEXT_PUBLIC_TERMINAL_WS_URL=ws://localhost:9084

# ==================== CUSTOM DOMAIN SUPPORT ====================
# When deploying to a custom domain (e.g., mon.go7s.net), configure these:
# NEXT_PUBLIC_DOMAIN=mon.go7s.net
# NEXT_PUBLIC_MONITORING_WS_URL=wss://mon.go7s.net/ws/monitoring
# NEXT_PUBLIC_TERMINAL_WS_URL=wss://mon.go7s.net/ws/terminal
# (These assume a reverse proxy like nginx/Caddy forwards /ws/* to backend)

# ==================== DEVELOPMENT ====================
# Node environment
NODE_ENV=development

# Allow access from LAN IP addresses (e.g., 192.168.x.x, 172.x.x.x)
# Set to "true" to prevent Next.js "allowedDevOrigins" warnings when accessing
# from devices on your local network
# WARNING: Only use in development! Not needed in production.
# ALLOW_LAN=true

# Custom allowed dev origins (comma-separated list)
# If you need specific origins beyond localhost and LAN ranges
# Example: DEV_ALLOWED_ORIGINS=mon.go7s.net,mon.local:9081
# DEV_ALLOWED_ORIGINS=
