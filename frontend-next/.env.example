# Frontend Environment Configuration
# Copy this file to .env.local for local development

# ==================== BACKEND API TARGETS ====================
# Backend API URL (for server-side proxy)
API_PROXY_TARGET=http://localhost:9083

# WebSocket URLs (for client-side connections)
NEXT_PUBLIC_MONITORING_WS_URL=ws://localhost:9085
NEXT_PUBLIC_TERMINAL_WS_URL=ws://localhost:9084

# ==================== DEVELOPMENT ====================
# Allow access from LAN IP addresses (e.g., 192.168.x.x, 172.x.x.x)
# Set to "true" to prevent Next.js "allowedDevOrigins" warnings when accessing
# from devices on your local network
# WARNING: Only use in development! Not needed in production.
# ALLOW_LAN=true

# Custom allowed dev origins (comma-separated list)
# If you need specific origins beyond localhost and LAN ranges
# Example: DEV_ALLOWED_ORIGINS=example.local:9081,test.local:9081
# DEV_ALLOWED_ORIGINS=
